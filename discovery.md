# 探索式立项（Discovery Mode）

用户**不确定技术栈**时使用。只描述「要做什么」，AI **一步步补全**需求与技术选型，再 scaffold + 双文档。

**默认模式**：用户未给技术栈 → 走本流程，不直接 scaffold。

---

## 用户怎么说

```
@project-bootstrap 我想做一个 [描述要做什么]
```

示例：

- `@project-bootstrap 我想做一个旅行记账 App，要能离线、手机电脑都能用`
- `@project-bootstrap 帮我把 Excel 清洗脚本做成小工具`

---

## 五阶段流程

每阶段结束**向用户确认**后再进下一阶段。用 AskQuestion（有则用）或编号选项；**一次只问一个决策点**。

```
阶段 1 意图 → 阶段 2 约束 → 阶段 3 技术栈 → 阶段 4 脚手架 → 阶段 5 双文档
```

每轮回复开头重述：`阶段 X/5：[当前在做什么]`。

---

### 阶段 1/5 — 意图（从用户描述提取）

从首条消息提取，缺什么问什么（一次一问）：

| 维度 | 要问清的 |
|------|----------|
| 核心功能 | 用户要完成什么任务（3 条 bullet 以内） |
| 使用者 | 仅自己 / 小团队 / 公开 |
| 形态 | 网站 / 手机 App / 桌面 / CLI / API / 组合 |
| 数据 | 仅本地 / 要云同步 / 要多人协作 |

**输出**：一段「我理解你要做…」+ 确认是否正确。用户确认 → 阶段 2。

---

### 阶段 2/5 — 约束（补全非功能需求）

按相关性提问，**不必全问**，只问影响选型的：

1. 是否必须离线可用？
2. 是否需要登录/账号？
3. 部署环境：本地跑 / 自己的服务器 / 纯静态托管？
4. 语言偏好：无偏好 / 只会 Python / 只会 TS / …
5. 时间预期：今晚能用 / 一两周 / 长期维护？

**输出**：「约束清单」5 条以内。用户确认 → 阶段 3。

---

### 阶段 3/5 — 技术栈（AI 提案，用户选）

给出 **2～3 套方案**，格式固定：

```
推荐（默认）：[方案名]
- 前端/运行时：…
- 后端：…（或无）
- 存储：…
- 理由：一句话
- 代价：一句话

备选 A：…
备选 B：…
```

**选型原则**（与用户 CLAUDE.md 哲学一致）：

- 优先成熟依赖 + 胶水代码，不造轮子
- 能一个端口 dev 就不拆多服务
- 用户说「不确定」→ 选推荐项并说明可后改

用户选定（或同意推荐）→ 输出「技术栈摘要表」→ 阶段 4。

---

### 阶段 4/5 — 脚手架

按选定栈执行最小 scaffold：

1. 初始化仓库 / 目录
2. 安装依赖、创建入口文件
3. `package.json`（或等价）脚本：`dev` / `build` / `lint`
4. 能跑通一个最小 Hello（页面 / CLI / health endpoint）

**不向用户倾倒实现细节**；完成后报告：创建了哪些文件、怎么启动。

用户确认能跑（或跳过验证）→ 阶段 5。

---

### 阶段 5/5 — 双文档

按 [templates.md](templates.md) 生成：

- `CLAUDE.md` — 第九节填阶段 3 定的架构
- `CODEMAP.md` — 按阶段 4 实际目录
- 可选 `changelog/`、`.cursor/rules/i-have-adhd.mdc`

走 [SKILL.md](SKILL.md) 完成校验清单。

---

## 中断与变更

| 情况 | 处理 |
|------|------|
| 阶段 3 改主意 | 回到阶段 3 重出方案，不 scaffold |
| 阶段 4 后发现栈不对 | 停 scaffold，回阶段 3 |
| 用户中途给明确技术栈 | 跳过阶段 3 提案，直接记录并进入阶段 4 |
| 用户说「先别写代码」 | 停在阶段 3，只输出需求 + 栈摘要 |

---

## 阶段进度模板（每轮粘贴）

```markdown
**阶段 2/5 — 约束**

已确定：
- 要做：…
- 形态：…

这一步请你选：是否必须离线？
1. 必须（无网也能用）
2. 不需要（始终在线即可）
3. 不确定，你推荐
```

---

## 与「已知技术栈」模式的分叉

| 用户输入 | 走哪条路 |
|----------|----------|
| 给了完整技术栈 | SKILL.md「执行顺序」，跳过 Discovery |
| 只描述做什么 | 本文件 Discovery Mode |
| 两者都有 | 用用户指定的栈，Discovery 只补约束 |
